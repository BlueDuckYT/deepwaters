# Gradle
# Build your Java project and run tests with Gradle using a Gradle wrapper script.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  GRADLE_USER_HOME: $(Pipeline.Workspace)/.gradle
  sdsds: dsd

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'Start-Sleep -Seconds 30'
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      echo "##vso[task.setvariable variable=tags]git tag --sort=-creatordate"
      echo "##vso[task.setvariable variable=tag]$(tags)[0]"
- task: Cache@2
  inputs:
    key: 'gradle | "$(Agent.OS)"'
    restoreKeys: gradle
    path: $(GRADLE_USER_HOME)
  displayName: Gradle build cache

- script: |
    ./gradlew --build-cache build    
    # stop the Gradle daemon to ensure no files are left open (impacting the save cache operation later)
    ./gradlew --stop    
  displayName: Build
- task: UseGitVersion@5
  inputs:
    versionSpec: '5.0.0'
    includePrerelease: true
- task: Gradle@2
  inputs:
    workingDirectory: ''
    gradleWrapperFile: 'gradlew'
    gradleOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '1.8'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: '**/TEST-*.xml'
    tasks: 'build'
- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Pipeline.Workspace)/s/build/libs/'
    publishLocation: 'pipeline'

- task: DiscordPublisher@1
  inputs:
    channelId: '$(wehook_id)'
    webhookKey: '$(webhook_token)'
    embeds: |
      {
        "embeds": [
          {
            "title": "Version $(tag)",
            "description": "A new build was just created because of a push to the github.\nYou must install Minecraft 1.14 and all dependencies in order to run the mod.\n\n",
            "color": 3120127,
            "thumbnail": {
              "url": "https://cdn.discordapp.com/icons/695117761956937779/4e5dde3ad1c8254fb27e8316cdc30a37.webp"
            },
      
      
            "fields": [
              {
                "name": "Dependencies:",
                "value": "**Minecraft Forge** >= [28.2.0](https://files.minecraftforge.net/maven/net/minecraftforge/forge/index_1.14.4.html)\n**Obfuscate Library** >= [0.3.3](https://www.curseforge.com/minecraft/mc-mods/obfuscate/files/2831155)"
              }
              
            ]
          }
        ]
      }